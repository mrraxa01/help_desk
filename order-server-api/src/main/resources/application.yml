spring:
  application:
    name: order-server-api

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
    hikari:
      maximumPoolSize: 10
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

